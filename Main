import requests
import pymongo
from sqlalchemy import true

STARSHIPS_URL = "https://swapi.info/api/starships"


# Get all data from endpoint
def get_api_data(url):
    try:
        response = requests.get(url)
        response.raise_for_status()
        data = response.json()
        return data
    except (Exception,) as e:
        print(f"Error fetching data: {e}")
        return None

client = pymongo.MongoClient()
db = client["Starwars"]

#db.create_collection("Starships_url")

#db.Starships_url.insert_many(get_api_data(STARSHIPS_URL))

All_pilots_from_ships = db.Starships_url.find({}, {"_id":1, "pilots":1, "name":1})

# for p in All_pilots_from_ships:
#     #api_data = get_api_data(p)
#     for x in p["pilots"]:
#         print(get_api_data(x))


#def get_object_id():
    
